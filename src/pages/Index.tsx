
import { Layout } from "@/components/Layout"
import { BrowserRouter } from "react-router-dom"

const Index = () => {
  return (
    <BrowserRouter>
      <Layout />
    </BrowserRouter>
  )
}

export default Index
